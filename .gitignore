# .gitignore
node_modules/
coverage/
*.log
.env
.DS_Store

# build artifacts / registry auth (generated in CI)
*.tgz
.npmrc

# npm-check write backups (timestamped lockfile/package.json snapshots)
.backups/
