# .gitignore
node_modules/
coverage/
*.log
.env
.DS_Store

# build artifacts / registry auth (generated in CI)
*.tgz
.npmrc
# …but test fixtures ship a committed .npmrc (pnpm registry-config cases)
!tests/fixtures/**/.npmrc

# SBOM (CycloneDX) — generated in CI, published to Dependency-Track
sbom.json
sbom-prod.json

# SonarQube scanner working/cache dirs (generated in CI)
.scannerwork/
.sonar/

# npm-check write backups (timestamped lockfile/package.json snapshots)
.backups/
